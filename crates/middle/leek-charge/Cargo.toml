[package]
name        = "leek-charge"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Optional HIR pass that inserts static op-budget Charge instructions."

[dependencies]
leek-hir      = { workspace = true }
leek-span     = { workspace = true }

[dev-dependencies]
# The integration test charges HIR lowered from real source rather than a
# hand-built tree, so it needs the frontend stages that produce one.
leek-syntax = { workspace = true }
leek-parser = { workspace = true }

[lints]
workspace = true
