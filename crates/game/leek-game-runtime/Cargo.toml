[package]
name        = "leek-game-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Leek-wars fight engine: the fight builtins (getCell, getLife, …) operating through a GameHost, plus the reference world model (Fight, Entity) — the game-side equivalent of leek-runtime."

[dependencies]
bitflags     = { workspace = true }
leek-runtime = { workspace = true }
serde_json   = { workspace = true }

[dev-dependencies]
# The fight-constant catalog `src/consts_gen.rs` is generated from; the
# tests assert the generated values still match it.
leek-environment = { workspace = true }

[lints]
workspace = true
