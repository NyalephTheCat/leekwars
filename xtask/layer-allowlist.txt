# Known crate-layering violations accepted by `cargo xtask check-layers`.
#
# One edge per line: `<from-crate> -> <to-crate>: <justification>`.
# An entry covers every dependency kind (normal, build, dev) of that edge.
#
# This list must only shrink. The check fails on an entry whose edge no longer
# breaks the rule, so remove the line in the same change that fixes the edge.
# Do not add entries to get a new dependency past the gate; see "The layering
# rule" in docs/architecture.md for the alternatives.

# The runtime value model stores HIR definition ids (class refs, class
# instances, user functions). Fix: move the id type into `core` or give the
# runtime its own id type (ARCH-03).
leek-runtime -> leek-hir: value::types uses leek_hir::DefId in Value

# The recipe catalogue plans the formatter and lint pipeline steps. Fix: let
# tools register their own steps on top of the db layer (ARCH-03).
leek-recipes -> leek-fmt: recipes plan the Fmt step and FormattedArtifact
leek-recipes -> leek-lint: recipes plan the LintFindings step

# Every frontend/middle stage defines its own pipeline step and artifact types
# on top of the generic `leek-pipeline` substrate, which itself depends only on
# `core`. Fix: move the substrate down or the step definitions up (ARCH-03).
leek-lexer -> leek-pipeline: stage defines its pipeline step/artifacts
leek-syntax -> leek-pipeline: stage defines its pipeline step/artifacts
leek-parser -> leek-pipeline: stage defines its pipeline step/artifacts
leek-resolver -> leek-pipeline: stage defines its pipeline step/artifacts
leek-types -> leek-pipeline: stage defines its pipeline step/artifacts
leek-hir -> leek-pipeline: stage defines its pipeline step/artifacts
leek-mir -> leek-pipeline: stage defines its pipeline step/artifacts
leek-charge -> leek-pipeline: stage defines its pipeline step/artifacts
leek-complexity -> leek-pipeline: stage defines its pipeline step/artifacts

# Dev-dependency, two layers up: tests/empirical.rs checks the complexity
# estimates against op counts measured by running the native backend.
leek-complexity -> leek-backend-native: empirical tests measure real op counts
