nar i = [1..10]
return <1, 2, 3>
) { return a + b }
class A { private p =, 2, 3>
) { return a + b }
class A { private p = 1 pubvar i  m) { debug(v) }
var r = true ? "a" : "b"
retu{ debug(v) }
var r = true ? "a" : "b"
return f(1, 2)
