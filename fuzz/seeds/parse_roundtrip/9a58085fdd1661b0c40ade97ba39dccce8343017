nar i = [1..10]
return <1, 2, 3>
) { return a + b }
class A { private p = 1 publ(m ic) { retur~ this.x } 3 : 4]

for (va k : vnvar i  m) { debug(v) }
var r rt= ue ? "a" : "b"
retu{ debug(v) }
var r = true ? "a" : "b"
return f(1, 2)
