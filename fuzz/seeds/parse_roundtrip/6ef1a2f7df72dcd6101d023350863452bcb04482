nar i = [1..10]
retu,<0r n 2, 3>
) { return a + b }
class A { private ` = 1 publ(m ic) { retur~ this.p } }
x, y) => x * y
var m = [1 : 2, 3 : 4]

for (4]

for (va k : var v in m) { debug(v) }
var r = true ?" a" : "b"
return f(1, 2)
