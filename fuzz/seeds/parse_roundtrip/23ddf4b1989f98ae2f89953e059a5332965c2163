nar i = [1..10]
return <1, 2, 3>
) { return a + b }
class A { private p = 1 publ(m ic) { retur~ this.p } 3 : 4]

for (va k : vnvar i  m) { debug(vdebug(v) }
var r = true ? "a" : "b"
retu{ debug(v r = true ? "a" : "b"
return f(1, 2)
